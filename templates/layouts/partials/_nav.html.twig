{#<nav>#}
{#    <ul>#}
{#        <li><a href="#">Home</a> </li>#}
{#        <li></li>#}
{#    </ul>#}
{#</nav>#}



<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('app_home') }}">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                {% if app.user %}
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout
                        <span class="sr-only">(current)</span>
                    </a>



                    {% if "RESPONSABLE" in app.user.roles %}
                        {{ include('layouts/responsable/nav-items_responsable.html.twig') }}
                        {% else %}
                        {{ include('layouts/etudiant/nav-item_etudiant.html.twig') }}
                    {% endif %}

                {% else %}
                    <a class="nav-link" href="{{ path('app_register') }}">Register
                        <span class="sr-only">(current)</span>
                    </a>

                    <a class="nav-link" href="{{ path('app_login') }}">Login
                        <span class="sr-only">(current)</span>
                    </a>

                {% endif %}
            </li>



        </ul>

        {#
            <li class="nav-item">
                <a class="nav-link" href="#">Pricing</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">About</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="#">Action</a>
                    <a class="dropdown-item" href="#">Another action</a>
                    <a class="dropdown-item" href="#">Something else here</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Separated link</a>
                </div>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
        </form> #}
    </div>
</nav>